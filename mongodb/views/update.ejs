<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/mongodb/public/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 p-10 text-white">
        <div class="nav mb-10 text-blue-500"> 
            <a href="/read">View All User</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">Edit User</h1>
        <form class="w-full h-screen mt-4" action="/update/<%= user._id%>" method="post">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-xl outline-none" type="text" name="name" placeholder="User Name" value="<%= user.name %>">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-xl outline-none" type="text" name="email" placeholder="Email" value="<%= user.email %>">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-xl outline-none" type="number" name="contactno" placeholder="Contact" value="<%= user.contactno %>">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-xl outline-none" type="text" name="address" placeholder="Address" value="<%= user.address %>">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-xl outline-none" type="text" name="image" placeholder="Image URL" value="<%= user.image %>">
            <input class="px-5 py-2 bg-yellow-500 text-black rounded-xl ml-3" type="submit" value="Update User">
        </form>

    </div>
</body>
</html>